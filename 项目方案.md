# Humanoid-v5 强化学习项目方案（修订版）

## 一、算法选型

**PPO（Proximal Policy Optimization）**，理由：
- MuJoCo 连续控制任务的 SOTA 基线算法，稳定、易调参
- Humanoid 是高维动作空间（17维），PPO 在此类任务上表现优异
- 5M 步限制下，PPO 样本效率可接受
- 参考项目：[ProfessorNova/PPO-Humanoid](https://github.com/ProfessorNova/PPO-Humanoid)

备选方案：SAC（样本效率更高，但调参敏感）

---

## 二、技术架构

```
├── lib/
│   ├── agent.py         # Actor-Critic 网络定义（含独立 log_std）
│   ├── buffer.py        # 轨迹缓冲区 + GAE 计算
│   └── utils.py         # 参数解析、环境构建、归一化、录屏
├── train.py             # 训练主脚本（含随机种子固定 + 混合精度）
├── eval.py              # 测试脚本（支持 --seed 参数）
├── requirements.txt     # 依赖列表
├── checkpoints/         # 模型与归一化参数保存目录
├── logs/                # TensorBoard 日志
└── videos/              # 训练过程录像
```

### 网络结构设计

- **Actor（策略网络）：** MLP，隐藏层 `[256, 256, 256]`，Tanh 激活
  - 均值头：最后一层 Linear(256, 17) + Tanh（限定在 [-1, 1]）
  - **log_std：独立 `nn.Parameter`**（形状 `(1, 17)`，初始值 `-0.5`），不与输入状态耦合
- **Critic（价值网络）：** MLP，隐藏层 `[256, 256, 256]`，Tanh 激活
  - 最后一层 Linear(256, 1)，无激活函数（V(s) 可为任意实数）
- Actor 和 Critic 不共享层（独立网络，避免学习目标相互干扰）
- 动作采样：`Normal(mu, std)`，`rsample()` 重参数化
- 动作范围映射：Tanh 输出 [-1, 1] → 映射到环境动作范围 [-0.4, 0.4]

---

## 三、关键超参数（PPO）

### 3.1 环境与训练规模

| 参数 | 值 | 说明 |
|------|------|------|
| env_id | `Humanoid-v5` | 统一环境 |
| n_envs | **32** | 并行环境数量 |
| n_steps | 2048 | 每个环境每轮收集步数 |
| n_epochs | **76** | 训练轮数（向环境收集数据的次数） |
| total_timesteps | **5,000,000** | 总交互步数 = n_envs × n_steps × n_epochs = 32 × 2048 × 76 |

> 每轮收集：`32 × 2048 = 65,536` 条过渡样本。76 轮总计约 498 万步，在 5M 限制内。

### 3.2 PPO 算法参数

| 参数 | 值 | 说明 |
|------|------|------|
| train_iters | **10** | 同一批数据反复学习的次数 |
| batch_size | **256** | 每个 mini-batch 的大小 |
| gamma | **0.995** | 折扣因子 |
| gae_lambda | 0.95 | GAE 衰减参数 |
| clip_range | 0.2 | PPO 裁剪范围（对应策略更新幅度上限 ±20%） |
| target_kl | **0.01** | **KL 早停阈值**：若 KL 散度超过此值，停止用当前批数据更新 |
| ent_coef | 1e-3 | 熵系数（有 NormalizeObservation 时可适当降低） |
| vf_coef | 0.5 | 价值损失权重 |
| max_grad_norm | 0.5 | 梯度裁剪阈值 |

### 3.3 优化器参数

| 参数 | 值 | 说明 |
|------|------|------|
| learning_rate | 3e-4 | 初始学习率 |
| lr_schedule | **warmup(10 epoch) + cosine annealing** | 前 10 轮从 0 线性升至 3e-4，之后余弦退火至 0 |

---

## 四、PPO 核心机制简述

### 4.1 总损失函数

```
loss = policy_loss + vf_coef × value_loss - ent_coef × entropy

policy_loss = -min(ratio × adv, clip(ratio, 0.8, 1.2) × adv)
value_loss = MSE(V_new(s), return)
entropy    = 正态分布的熵（只取决于 σ）
```

### 4.2 ratio 的含义

```
ratio = π_new(a|s) / π_old(a|s)
      = exp(new_log_prob - old_log_prob)

ratio > 1: 新策略更"认可"这个动作
ratio < 1: 新策略更"排斥"这个动作
clip:  强制 ratio ∈ [0.8, 1.2]，每次更新策略变化不超过 20%
```

### 4.3 GAE（Generalized Advantage Estimation）

```
δ_t  = r_t + γ × V(s_{t+1}) - V(s_t)              ← 单步 TD 误差
A_t  = δ_t + (γλ)δ_{t+1} + (γλ)²δ_{t+2} + ...    ← 指数加权未来 TD 误差
     = δ_t + γλ × mask × A_{t+1}                  ← 递推实现 (O(n))

mask = term_mask × trunc_mask
  terminated → term_mask=0, 未来价值归零
  truncated  → trunc_mask=0, GAE 不自举继承

return = advantage + value
```

### 4.4 KL 早停

```
每轮 mini-batch 更新后计算 KL(new_policy || old_policy)
若 KL > target_kl(0.01): 跳出所有 mini-batch，停止用当前批数据更新
→ 等价于"自适应 train_iters"，防止策略更新过度
```

---

## 五、训练策略

### 5.1 环境处理

- 使用 `VecNormalize` 包装器做 **Observation + Reward 归一化**
- 训练结束时保存 `vec_normalize.pkl`（含 running mean/var/count），测试时加载
- 不使用额外的 Reward Shaping（以保证与原生奖励一致，满足作业要求）

### 5.2 随机种子固定

```python
RANDOM_SEED = 42
random.seed(SEED)
np.random.seed(SEED)
torch.manual_seed(SEED)
torch.cuda.manual_seed_all(SEED)
torch.backends.cudnn.deterministic = True
torch.backends.cudnn.benchmark = False
# env.reset(seed=SEED) 在训练循环中设置
```

### 5.3 混合精度训练（可选）

使用 `torch.amp.GradScaler` 加速训练并节省显存：
- 前向用 float16 加速
- loss 放大防止梯度下溢
- `scaler.step(optimizer)` 更新参数

### 5.4 评估与保存策略

- 每 5 万步做一次评估（10 episodes），使用确定性策略（取 mean）
- 评估时用**原生环境**（无归一化 wrapper），记录原始累计奖励
- 保存 best model（按评估 reward 最高）+ 每轮 last model
- 每 10 轮录制一段视频到 `videos/` 目录

---

## 六、预期结果

| 指标 | 目标值 |
|------|--------|
| 原始累计奖励 | 5000+ |
| 存活步数 | 接近 1000（满回合） |
| 前向移动速度 | 持续正向位移 |

---

## 七、实施计划

| 阶段 | 任务 | 预计耗时 |
|------|------|----------|
| 1 | 搭建基础框架（lib/agent, lib/buffer, lib/utils）+ 环境对接 + 版本对齐 | 1天 |
| 2 | 实现 train.py（PPO 收集/更新循环 + 随机种子 + 归一化 + 日志） | 2天 |
| 3 | 调通训练 + 超参数调优 + 多次训练 | 2-3天 |
| 4 | 实现 eval.py（支持 --seed 参数 + 加载归一化参数 + 输出原始累计奖励） | 0.5天 |
| 5 | 编写报告（PDF）+ 分数截图 + 训练录屏 | 1天 |
| 6 | 最终验证 + 打包提交 | 0.5天 |

---

## 八、依赖文件 `requirements.txt`

```
gymnasium==1.2.3
mujoco==3.8.1
torch==2.4.0
numpy==1.26.4
tensorboard==2.17.0
opencv-python>=4.11.0
tqdm>=4.67.0
```

---

## 九、与作业要求的对照检查

| 作业要求 | 状态 | 方案对应措施 |
|------|------|------|
| Humanoid-v5 | ✅ | env_id 固定 |
| gymnasium==1.2.3, mujoco==3.8.1 | ✅ | requirements.txt 锁定版本 |
| 随机种子固定 | ✅ | 全流程 seed 固定 + env.reset(seed=...) |
| 禁止修改物理参数 | ✅ | 不修改环境 XML 和物理常量 |
| 原生环境累计奖励评测 | ✅ | 训练用归一化 wrapper，评测用原生环境 |
| 步数 ≤ 5,000,000 | ✅ | 32 × 2048 × 76 ≈ 498 万 |
| 归一化参数保存/加载 | ✅ | VecNormalize 的 mean/var/count 保存为 pkl |
| eval 支持 --seed 参数 | ✅ | eval.py 命令行传参 |
| requirements.txt 含版本号 | ✅ | 所有依赖标注版本 |
